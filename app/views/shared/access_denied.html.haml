.text-center
  %h1 #{flash[:error]}
.alert.alert-danger.alert-dismissable
  #errorExplanation.errorExplanation
    %h4 Access to the request page has been denied
    %p You do not have sufficient credentials to access
    = link_to 'Back',request.referrer || root_path